# 考研背诵系统 - 虚拟主机部署指南

## 文件结构
```
├── index.html      # 前台页面
├── api.php         # 前台API
├── admin.html      # 后台管理页面
├── admin.php       # 后台管理API
├── config.php      # 数据库配置文件
└── database.sql    # 数据库初始化脚本
```

## 部署步骤

### 1. 创建数据库
登录虚拟主机面板，创建MySQL数据库：
- 数据库名：`recite_system`（或自定义名称）
- 用户名：创建数据库用户
- 密码：设置密码

### 2. 导入数据库
在phpMyAdmin中导入 `database.sql` 文件

### 3. 修改配置文件
编辑 `config.php`：
```php
return [
    'host' => 'localhost',           // 数据库主机（一般不用改）
    'dbname' => '你的数据库名',       // 修改为你的数据库名
    'username' => '你的数据库用户名', // 修改为你的数据库用户名
    'password' => '你的数据库密码',   // 修改为你的数据库密码
    'charset' => 'utf8mb4',
    'admin_password' => 'admin123'   // 后台管理密码
];
```

### 4. 上传文件
将所有文件上传到虚拟主机的网站根目录

### 5. 访问系统
- 前台：http://你的域名/
- 后台：http://你的域名/admin.html
- 后台密码：admin123（可在后台修改）

## 后台管理功能

访问 `http://你的域名/admin.html`，输入管理密码登录。

### 仪表板
- 查看用户数量、卡片数量、复习次数等统计
- 查看系统信息（PHP版本、MySQL版本等）

### 用户管理
- 查看所有用户列表
- 编辑用户昵称
- 删除用户（不能删除管理员）

### 数据库管理
- 修改数据库配置
- 备份数据库
- 优化表、检查表
- 执行SQL语句

### 系统设置
- 修改后台管理密码
- 修改系统名称
- 清除所有数据
- 重置系统

## 常见问题

### Q: 如何修改数据库名？
A: 编辑 `config.php` 文件，修改 `dbname` 的值

### Q: 如何修改后台密码？
A: 登录后台，在"系统设置"中修改，或直接编辑 `config.php` 中的 `admin_password`

### Q: 忘记后台密码怎么办？
A: 直接编辑 `config.php` 文件，修改 `admin_password` 的值

### Q: 如何备份数据？
A: 登录后台，在"数据库管理"中点击"备份数据库"

## 安全建议

1. 部署后立即修改后台管理密码
2. 定期备份数据库
3. 不要将 `config.php` 文件暴露给他人
4. 使用强密码作为数据库密码
